import 'package:flutter/material.dart';

class DoctorsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Doctores'),
      ),
      body: Center(
        child: Text('Hola Doctors'),
      ),
    );
  }
}
