import 'package:flutter/material.dart';

class MapsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hospitales y farmacias'),
      ),
      body: Center(
        child: Text('Hola Maps'),
      ),
    );
  }
}
